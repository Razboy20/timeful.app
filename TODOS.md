TODO: Strip PII from group event information (`enabledCalendars`), especially for unauthed users.
  Route: https://timeful.longhorns.dev/api/events/:id
TODO: Check security of API endpoints
TODO: Create random session secret on startup rather than hardcoding it as an environment variable, if it is not already set.
